var barClass = <%= @project.bar_class %>;

var bar = $('#p-bar');
bar.attr({"aria-valuenow":<%= @project.progress %>, "style":"width: <%= @project.progress %>%"});
bar.removeClass().addClass(barClass).text("<%= @project.progress %>%");
