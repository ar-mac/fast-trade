%nav.navbar.navbar-inverse.navbar-fixed-bottom
  .container
    %ul.nav.navbar-nav
      %li.dropdown
        %a.dropdown-toggle{"aria-expanded" => "false", "data-toggle" => "dropdown", :href => "#", :role => "button"}
          = t('links.locale.choose')
          %span.caret
        %ul.dropdown-menu{:role => "menu"}
          %li
            = link_to locale_path(locale: 'pl') do
              = image_tag('poland.gif', size: "32x24")
              = t('links.locale.polish')
          %li
            = link_to locale_path(locale: 'en') do
              = image_tag('britain.gif', size: "32x24")
              = t('links.locale.english')
          %li
            = link_to locale_path(locale: 'es') do
              = image_tag('spain.gif', size: "32x24")
              = t('links.locale.spanish')
          