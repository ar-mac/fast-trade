- breadcrumb :home

.jumbotron
  %h1= t('elements.welcome')
  %p= t('elements.jumbo')
  = link_to t('links.user.register'), new_user_path, class: 'btn btn-primary'
  %span= t('elements.or')
  = link_to t('links.user.login'), login_path, class: 'btn btn-primary'

.row
  .col-md-4.col-md-offset-4
    = render 'shared/search_form'
